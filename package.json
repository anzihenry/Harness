{
  "name": "harness",
  "version": "0.2.0",
  "description": "A tool for managing agent assets with versioning and agent-specific adaptation.",
  "type": "module",
  "bin": {
    "harness": "./src/cli.js"
  },
  "scripts": {
    "start": "node ./src/cli.js",
    "test": "node --test ./tests/*.test.js",
    "check": "node --check ./src/cli.js && node --check ./src/core/workspace.js && node --check ./src/core/adapters.js && node --check ./src/core/paths.js && node --check ./src/utils/diff.js && node --check ./src/utils/json.js && npm run test"
  },
  "keywords": [
    "agent",
    "assets",
    "skills",
    "instructions",
    "versioning"
  ],
  "license": "MIT"
}
